# ── Gateway: web framework ──────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0

# ── Gateway: data + infra ───────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
redis>=5.0.0
aio-pika>=9.4.0
minio>=7.2.0

# ── Gateway: security ───────────────────────────────────────────────────────
bcrypt>=4.1.0
PyJWT>=2.9.0

# ── Observability (Sprint 4B, ADR-010) ─────────────────────────────────────
# Prometheus client for the /metrics scrape endpoint and instrument registry.
# Justification: the stdlib has no metrics/exposition format; prometheus-client
# is the reference implementation (pure Python, no C deps) for the Prometheus
# text format our collector scrapes. Used behind the observability facade only.
prometheus-client>=0.20.0

# OpenTelemetry SDK + OTLP/HTTP exporter for distributed tracing. Tracing only
# (spans + W3C traceparent propagation); metrics stay on Prometheus per ADR-010.
# Justification: the stdlib has no tracing or trace-context propagation;
# OpenTelemetry is the vendor-neutral standard chosen in ADR-010, and the
# collector is the config-only swap point for the trace backend (Collector /
# Tempo / Jaeger). Used behind the observability facade only.
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0

# ── Worker: ML pipeline ─────────────────────────────────────────────────────
tensorflow>=2.16.0
opencv-python-headless>=4.9.0
pydicom>=2.4.0
numpy>=1.26.0
Pillow>=10.0.0
scikit-image>=0.24.0  # golden-image regression gates (PSNR / SSIM)
