# Grafana provisioning — Prometheus datasource (Sprint 4B observability overlay).
# Mounted read-only into the Grafana container; no manual datasource setup needed.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
