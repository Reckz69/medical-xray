[pytest]
asyncio_mode = auto
asyncio_default_test_loop_scope = session
asyncio_default_fixture_loop_scope = session
testpaths = tests
pythonpath = .
addopts = -m "not golden"
markers =
    golden: golden-image regression gates; run explicitly with `pytest -m golden`
